<Window x:Class="VkGetIds.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VkGetIds"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBox x:Name="textBoxForID"   AcceptsReturn="True" ToolTip="Сюда закидывать ссылки из таблицы" Margin="10,10,10,64" TextWrapping="Wrap" FontFamily="Courier New"/>
        <Button Content="Получить каноничные адреса" Margin="10,0,10,10" Click="Button_Click" Grid.ColumnSpan="2" Height="49" VerticalAlignment="Bottom"/>
        <TextBox x:Name="textboxOutput" AcceptsReturn="True" Grid.Column="1" Margin="10,10,10,64" TextWrapping="Wrap" AutomationProperties.HelpText="умаыукамыуам" ToolTip="Здесь будет вывод"/>
    </Grid>
</Window>
